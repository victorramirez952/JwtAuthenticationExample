## JwtAuthenticationExample

Proyecto MVC de ejemplo con autenticación JWT usando Microsoft.AspNetCore.Authentication.JwtBearer.

El proyecto crea una base de datos en memoria para almacenar información.

Las claves de autenticación están hardcodeadas en el código para simplificar el ejemplo.
